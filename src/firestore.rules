rules_version = '2';
service cloud.firestore {
match /databases/{database}/documents {
// כלל גורף: מאפשר קריאה וכתיבה (כולל מחיקה) לכל משתמש מחובר
match /{document=**} {
allow read, write: if request.auth != null;
}
}
}